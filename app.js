const add = require('./add');
const subtract = require('./subtract');
const multiply = require('./multiply');
const divide = require('./divide');

add(10, 5);
subtract(10, 5);
multiply(10, 5);
divide(10, 5);
divide(10, 0); // Division by zero test
